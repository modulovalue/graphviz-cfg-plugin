digraph CFG{node[shape=box,fontname="Courier"];entry;subgraph cluster_if{label="if/else";cmp;then;els;cmp->then[cfgbias=left];cmp->els[cfgbias=right]} subgraph cluster_loop{label="loop";head;body;head->body[cfgbias=left];body->head} entry->cmp;then->head;els->head;head->ret[cfgbias=right]}
